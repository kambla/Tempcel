# Configuration values for the chart

image:
  repository: tempcel

# Port exposed by the service
service:
  port: 5000 # Sesired port

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80
